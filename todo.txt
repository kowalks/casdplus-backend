
	TODO
	- CASDvest ou CASDinho no GET /student/
    - Tirar classes de GET /student/
    - GET /student/classes/
    - Passar token admin na MessageController.store
    - Passar token admin em StudentController.store
    - Passar token admin em StudentController.bulk_store
    - Passar token admin em ClassController.store
    - DELETE /student/login
    - GET /student/faltas
    - GET /admin/classes

    # Tipo de Messages
    - criar tabela de categorias
    - alimentar com as seeds que a gente escolheu
    - fazer o GET /admin/categorias
    - adicionar chave estrangeira na tabela message
    - mudar os seeds
    - mudar o POST /admin/classes/:class_id/message
    - conferir se ta vindo no GET

    # Filtro de Mensagens
    - GET /student/messages?tipo=:tipo
    - GET /student/messages?start_date=:start_date&end_date=:end_date

    # Pin
    - adicionar atributo na tabela message
    - mudar os seeds com o atributo novo
    - GET /admin/messages
    - PATCH /admin/messages/:message_id


    NÃO IMPORTANTE
	- Mensagem de erro no bulk_store do student