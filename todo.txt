
	TODO
	K+ CASDvest ou CASDinho no GET /student/
    K+ Tirar classes de GET /student/
    K+ GET /student/classes/
    K+ Passar token admin na MessageController.store
    K+ Passar token admin em StudentController.store
    K+ Passar token admin em StudentController.bulk_store
    K+ Passar token admin em ClassController.store
    K+ DELETE /student/login
    K- GET /student/faltas
    K+ GET /admin/classes

    # Tipo de Messages
    Y+ criar tabela de categorias
    Y+ alimentar com as seeds que a gente escolheu (criar as categorias)
    Y+ fazer o GET /admin/categorias
    Y+ adicionar chave estrangeira na tabela message
    Y+ mudar os seeds (adicionar as categorias nas mensagens existentes)
    Y+ mudar o POST /admin/classes/:class_id/message
    Y+ conferir se ta vindo no GET

    # Filtro de Mensagens
    K- GET /student/messages?tipo=:tipo
    K- GET /student/messages?start_date=:start_date&end_date=:end_date

    # Pin
    Y+ adicionar atributo na tabela message
    Y+ mudar os seeds com o atributo novo
    Y+ GET /admin/messages
    Y+ PATCH /admin/messages/:message_id


    NÃO IMPORTANTE
	- Mensagem de erro no bulk_store do student
    - Validar melhor o email colocado no POST /admin/students_bulk
    - validar melhor o email colocado no POST /admin